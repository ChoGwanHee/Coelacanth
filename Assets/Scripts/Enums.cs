
public enum PlayerAniState
{
    Idle,
    MovableAttack,
    Attack,
    KnockBack,
    Stun,
    Fall,
    Emotion
}

public enum FireworkType
{
    Fist,
    Roman,
    Fountain,
    Rocket,
    Butterfly,
    Party,
    Sparkling
}

public enum GameState {
    Wait,
    Playing,
    Result
}

public enum GameEvent
{
    GameStart,
    GameStop
}